# ==============================================================================
# OCR-MCP Server Environment Configuration
# ==============================================================================

# --- 1. OCR AI (Handwriting Recognition) ---
# Service: Primarily Qwen-VL (via Alibaba DashScope or OpenRouter)
# Purpose: Converts handwritten essay images/PDFs into raw text.
QWEN_API_KEY=your_qwen_api_key_here
QWEN_API_MODEL=qwen-vl-max
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# --- 2. CLEANING AI (Text Normalization) ---
# Service: Primarily xAI (Grok) or OpenAI (GPT-4o)
# Purpose: Optional pass to fix OCR typos and formatting artifacts.
CLEANING_API_KEY=your_xai_or_openai_key_here
CLEANING_API_MODEL=grok-beta
CLEANING_BASE_URL=https://api.x.ai/v1

# --- 3. EVALUATION AI (Grading & Feedback) ---
# Service: Primarily xAI (Grok) or OpenAI (GPT-4o)
# Purpose: Grades essays based on rubrics and provided context.
EVALUATION_API_KEY=your_xai_or_openai_key_here
EVALUATION_API_MODEL=grok-beta
EVALUATION_BASE_URL=https://api.x.ai/v1

# --- 4. SYSTEM SETTINGS ---
# Database path (default is edmcp.db in project root)
DB_PATH=edmcp.db
# Base directory for job artifacts (PDFs, Logs)
JOBS_DIR=data/jobs

# --- 5. SMTP CONFIGURATION (Email Student Reports) ---
# Service: Brevo SMTP Relay (or any SMTP provider)
# Purpose: Sends individual PDF feedback reports to students via email.
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_TLS=true
SMTP_USER=your_smtp_username_here
SMTP_PASS=your_smtp_password_here
FROM_EMAIL=your_sender_email@domain.com
FROM_NAME="Your Name or Organization"
